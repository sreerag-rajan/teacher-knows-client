// import { useSelector } from "react-redux"
import "./Home.css"

export const Home = ()=>{
  // const {user} = useSelector(store=>store.auth)

  return(
    <div>
      <h1>Teacher Knows</h1>
    </div>
  )
}